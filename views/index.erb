<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>単語帳</title>
    <meta name="viewpoint" content="width=device-width, initial-scale=1">
    <link rel ="stylesheet" href="/stylesheets/wordbox.css">
</head>
<body>

<div>
    <form action="/confirm" method="post">
    　　<input type="submit" value="アンケートを作る">
    </form>
 </div>
 
  <main>
     <% @contents.each do |content| %>
     <article>
         <h2><%= content.title %></h2>
         <p><%= content.body %></p>
         <span><%= content.good %></span>
         
         <span>
         <form action="/good/<%= content.id %>" method="post"><input type="text" placeholder="コメント" name= "comment">
         <label><input type="radio" name="agree" id="agree" value="賛成" checked>賛成</label>
         <label><input type="radio" name="agree" id="disagree" value="反対" checked>反対</label>
         <input type="submit" value="投票">
         </form>
         </span>
         
         <div class="edit">
            <form action="/detail/<%= content.id %>" method="post"><input type="submit" value="詳細"></form>
            <form action="/edit/<%= content.id %>" method="post"><input type="submit" value="編集"></form>
            <form action="/delete/<%= content.id %>" method="post"><input type="submit" value="削除"></form>
         </div>
     </article>
     <% end %>
 </main>
 
 </body>
</html>
